.env
__pycache__/
*.pyc
node_modules/
.venv/
venv/
.DS_Store

# Data files
# We want to keep the structure but ignore the actual JSONs if they are many
# However, for a small archive, it's better to keep them in git.
# Let's only ignore local environment files.
